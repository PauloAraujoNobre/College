import pessoa
from pessoa import Pessoa

class Banco:
    def __init__(self,numero,pessoa)
        self.numero = numero
        if pessoa.tipo == "Pessoa Juridica":
            self.pesso = pesso
        else:
            print("Erro, a pessoa não é juridica")
        
    def tipoCliente(self):
        op = input("Escolha o tipo de cliente\n1 - Cliente PF\n2 - Cliente PJ")
        if op == 1:
            self.cliente = "Cliente_PF"
        else:
            self.cliente = "Cliente_PJ"
